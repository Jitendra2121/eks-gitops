node {
    stage('Downstream') {
        properties([
            parameters([string(
                defaultValue: '', 
                description: '', 
                name: 'Latest_Build_Number', 
                trim: false)
            ])
        ])
        echo Latest_Build_Number
    }
}
